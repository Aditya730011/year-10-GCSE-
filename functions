

def calculate_sale_price(original_price,discount_percentage):
    final_price = original_price * (1 - (discount_percentage/100))
    print(final_price)



original_price = int(input("price"))
discount_percentage = int(input(""))
calculate_sale_price(original_price,discount_percentage)







cost = 0
def get_ticket_cost(i):
    if i < 5:
        cost += 0
    elif i >= 5 and i <= 17:
        cost += 10
    else:
        cost+=20
    print(cost)


age= int(input("age"))
get_ticket_cost(age)
        
